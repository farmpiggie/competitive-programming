// bob always chooses max(0, (subset) - k) cheapest items to buy (k[i])
// if alice chooses <= k items, her profit is negative
// she wants to choose items that are expensive in a[j] so that bob needs to buy them?
// (subset - k) cheapest items in a[j] should try to offset prices of all other items
#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int t; cin >> t;
	while (t--) {

	}
	return 0;
}
