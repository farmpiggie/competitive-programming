#include <bits/stdc++.h>
using namespace std; 
long long T, a, b, c, d;
int main() {
    cin >> T;
    for (int i = 0; i < T; i++) {
        cin >> a >> b >> c >> d;
        cout << b << " " << c << " " << c << endl;
    }
    return 0;
}