#include <bits/stdc++.h>
using namespace std;
int T, X;

int main() {
    cin >> T; // up to 100
    while (T--) {
        cin >> X;
        cout << 1 << " " << X - 1 << endl;
    }
    return 0;
}
