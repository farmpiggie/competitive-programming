/*
 * find the # of bridges that exist
 * for each bridge split the graph.
 * we want to see if theres a path from each CC from one bridge to the other bridge in the CC.
 * should be like
 * CC -> bridge -> CC -> bridge, no loops
 */


